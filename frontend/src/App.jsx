import React, { useState, useEffect } from 'react'; import axios from './api'; const App = () => { const [tasks, setTasks] = useState([]); const [task, setTask] = useState(''); const fetchTasks = async () => { const response = await axios.get('/api/tasks'); setTasks(response.data); }; const addTask = async () => { await axios.post('/api/tasks', { name: task }); setTask(''); fetchTasks(); }; const deleteTask = async (id) => { await axios.delete(`/api/tasks/${id}`); fetchTasks(); }; const toggleTask = async (id) => { await axios.patch(`/api/tasks/${id}`); fetchTasks(); }; useEffect(() => { fetchTasks(); }, []); return (<div><h1>Todo App</h1><input value={task} onChange={(e) => setTask(e.target.value)} /><button onClick={addTask}>Add Task</button><ul>{tasks.map(t => (<li key={t._id}>{t.name} <button onClick={() => toggleTask(t._id)}>Toggle</button> <button onClick={() => deleteTask(t._id)}>Delete</button></li>))}</ul></div>); }; export default App;